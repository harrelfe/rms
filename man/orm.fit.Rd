% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orm.fit.r
\name{orm.fit}
\alias{orm.fit}
\title{Ordinal Regression Model Fitter}
\usage{
orm.fit(
  x = NULL,
  y,
  cluster = NULL,
  family = c("logistic", "probit", "loglog", "cloglog", "cauchit"),
  offset,
  initial,
  opt_method = c("NR", "LM"),
  maxit = 30L,
  eps = 5e-04,
  gradtol = 0.001,
  abstol = 1e+10,
  minstepsize = 0.01,
  tol = .Machine$double.eps,
  trace = FALSE,
  penalty.matrix = NULL,
  weights = NULL,
  normwt = FALSE,
  scale = FALSE,
  mscore = FALSE,
  inclpen = TRUE,
  y.precision = 7,
  compstats = TRUE,
  onlydata = FALSE,
  sigma.init = 1,
  maxit.outer = 100L,
  maxit.mode = 30L,
  nAGQ = 7L,
  nAGQ.grid = c(7L, 11L, 15L, 21L, 31L, 45L, 63L),
  nAGQ.tol = 1e-05,
  mre = NULL,
  ...
)
}
\arguments{
\item{x}{design matrix with no column for an intercept}

\item{y}{response vector, numeric, factor, or character. The
ordering of levels is assumed from \code{factor(y)}.}

\item{cluster}{optional vector, the same length as \code{y}, of cluster
identifiers (e.g. subject ID for repeated measurements). When
given, \code{orm.fit} fits an additional normally-distributed random
intercept for each distinct cluster; see \strong{Details}. Left as
\code{NULL} (the default) to fit the ordinary fixed-effects model.}

\item{family}{a character value specifying the distribution family,
corresponding to logistic (the default), Gaussian, Cauchy, Gumbel
maximum (\code{exp(-exp(-x))}; extreme value type I), and Gumbel
minimum (\code{1-exp(-exp(x))}) distributions. These are the
cumulative distribution functions assumed for
\code{Prob[Y >= y | X]}. The \code{family} argument can be an unquoted or a
quoted string, e.g. \code{family=loglog} or \code{family="loglog"}. To use
a built-in family, the string must be one of the following,
corresponding to the previous list: \code{logistic}, \code{probit},
\code{loglog}, \code{cloglog}, \code{cauchit}.}

\item{offset}{optional numeric vector containing an offset on the
logit scale}

\item{initial}{vector of initial parameter estimates, beginning
with the intercepts. If \code{initial} is not specified, the function
computes the overall score \eqn{\chi^2} test for the global null
hypothesis of no regression. \code{initial} is padded to the right
with zeros for the regression coefficients, if needed. When
censoring is present, \code{initial} can also be a list with elements
\code{time} and \code{surv} from the \code{npsurv} attribute of the \code{y} element
of a previous fit. This is useful when bootstrapping, for
example. When \code{cluster} is given, \code{orm.fit}'s own fixed-effects
initial-value refinement (run once, regardless of \code{cluster}) is
reused as the warm start for the random-intercept fit, so
\code{initial} need not account for random effects.}

\item{opt_method}{set to \code{"LM"} to use Levenberg-Marquardt instead
of the default Newton-Raphson; see \strong{Details} for the asymmetric
damping schedule used.}

\item{maxit}{maximum no. iterations (default=\code{30}). For the
random-intercept outer loop this is instead governed by
\code{maxit.outer}.}

\item{eps}{difference in \eqn{-2 \log} likelihood for declaring
convergence. Default is \code{.0005}. This handles the case where the
initial estimates are MLEs, to prevent endless step-halving.}

\item{gradtol}{maximum absolute gradient before convergence can be
declared. For the fixed-effects fit, \code{gradtol} is automatically
scaled by \code{n / 1000} since the gradient is proportional to the
sample size; for the random-intercept outer loop it is used as an
absolute tolerance instead (see \strong{Details}).}

\item{abstol}{maximum absolute change in parameter estimates from
one iteration to the next before convergence can be declared; by
default has no effect for the fixed-effects fit. For the
random-intercept outer loop, this is capped at \code{1e-4} internally
regardless of the value supplied, since the default of \code{1e10} was
found to make this criterion a permanent no-op for that loop's
only-linear convergence.}

\item{minstepsize}{used to specify when to abandon step-halving}

\item{tol}{singularity criterion. Default is typically \code{2e-16}}

\item{trace}{set to \code{TRUE} to print \code{-2} log likelihood,
step-halving fraction, change in \code{-2} log likelihood, maximum
absolute value of first derivative, and max absolute change in
parameter estimates at each iteration. For the random-intercept
outer loop, set to a value greater than \code{1} to also print
per-outer-iteration mode-finding and AGQ diagnostics.}

\item{penalty.matrix}{a self-contained ready-to-use penalty matrix
-- see \code{lrm}}

\item{weights}{a vector (same length as \code{y}) of possibly fractional
case weights}

\item{normwt}{set to \code{TRUE} to scale \code{weights} so they sum to \code{n},
the length of \code{y}; useful for sample surveys as opposed to the
default of frequency weighting}

\item{scale}{set to \code{TRUE} to subtract column means and divide by
column standard deviations of \code{x} before fitting, and to
back-solve for the un-normalized covariance matrix and regression
coefficients. This can sometimes make the model converge for very
large sample sizes where for example spline or polynomial
component variables create scaling problems leading to loss of
precision when accumulating sums of squares and crossproducts.}

\item{mscore}{set to \code{TRUE} to compute the sparse score matrix and
store its elements as a list \code{mscore}. Not currently computed
when \code{cluster} is given.}

\item{inclpen}{set to \code{FALSE} to not include the penalty matrix in
the Hessian when the Hessian is being computed on transformed
\code{x}, vs. adding the penalty after back-transforming. This should
not matter.}

\item{y.precision}{when \code{y} is numeric, values may need to be
rounded to avoid unpredictable behavior with \code{unique()} with
floating-point numbers. Default is to 7 decimal places.}

\item{compstats}{set to \code{FALSE} to prevent the calculation of the
vector of model statistics}

\item{onlydata}{set to \code{TRUE} to return the data used in model
fitting as a list, without fitting the model}

\item{sigma.init}{starting value for the random-intercept standard
deviation \code{sigma}, used only when \code{cluster} is given}

\item{maxit.outer}{maximum number of outer-loop iterations for the
random-intercept fit (mode-finding / AGQ-reweighted Newton step /
\code{sigma} profile search), used only when \code{cluster} is given}

\item{maxit.mode}{maximum number of Newton-Raphson iterations for
the per-cluster random-intercept mode-finding inner loop, used
only when \code{cluster} is given}

\item{nAGQ}{starting number of adaptive Gauss-Hermite quadrature
nodes for the random-intercept fit, used only when \code{cluster} is
given; escalated automatically as needed (see \strong{Details})}

\item{nAGQ.grid}{increasing sequence of candidate quadrature-node
counts \code{nAGQ} is escalated through, used only when \code{cluster} is
given}

\item{nAGQ.tol}{relative log-likelihood change, comparing the
current \code{nAGQ} to the next candidate in \code{nAGQ.grid}, below which
\code{nAGQ} escalation stops; used only when \code{cluster} is given}

\item{mre}{optional fixed, numeric vector the same length as \code{y},
used only when \code{cluster} is also given: a user-computed
"multiplier for random effects" governing how the random
intercept's own contribution is weighted at each observation, via
two estimated scale parameters \code{sigma1} and \code{sigma2} in place of
the ordinary single \code{sigma} (see \strong{Details}). Left as \code{NULL}
(the default) to fit the ordinary, single-\code{sigma} random-
intercept model. For a Markov-1 model on repeated ordinal
responses, a natural choice is \code{mre} equal to \code{0} at each
subject's first follow-up and \code{1} at every later one (see
\strong{Examples}), but any fixed function of the data, continuous or
discrete, is valid. \code{mre} must vary within at least some
clusters; a completely constant \code{mre} (whether \code{0}, \code{1}, or any
other single value across the whole dataset) is rejected with an
informative error, since it would leave \code{sigma1} and \code{sigma2}
unidentifiable.}

\item{...}{ignored}
}
\value{
a list with the following components, not counting all the
components produced by \code{orm.fit}:
\itemize{
\item \code{call}: calling expression
\item \code{freq}: table of frequencies for \code{y} in order of increasing \code{y}
\item \code{yunique}: vector of sorted unique values of \code{y}
\item \code{stats}: vector with the following elements: number of
observations used in the fit, effective sample size (\code{"ESS"}; see
\strong{Details} for a limitation when \code{cluster} is given), number of
unique \code{y} values, median \code{y} from among the observations used in
the fit, maximum absolute value of first derivative of log
likelihood, model likelihood ratio chi-square, d.f., P-value,
score chi-square and its P-value, Spearman's \eqn{\rho} rank
correlation between linear predictor and \code{y} (if there is no
censoring), Somers' \eqn{Dxy} rank correlation (if there is no
censoring or only right censoring), the Nagelkerke \eqn{R^2}
index, other \eqn{R^2} measures, the \eqn{g}-index, \code{gr} (the
\eqn{g}-index on the ratio scale), and \code{pdm} (the mean absolute
difference between 0.5 and the estimated probability that
\eqn{y \geq} the marginal median). When \code{penalty.matrix} is
present, the chi-square, d.f., and P-value are not corrected for
the effective d.f. When \code{cluster} is given, the score-test-related
elements are not currently available (see \strong{Details}) and come
back as \code{NA}.
\item \code{fail}: set to \code{TRUE} if convergence failed (and \code{maxit>1}, or,
when \code{cluster} is given, \code{maxit.outer>1})
\item \code{coefficients}: estimated parameters (intercepts then slopes)
\item \code{family}, \code{famfunctions}: see \code{\link[=orm]{orm()}}
\item \code{deviance}: named vector of \code{-2} log likelihoods, in the order
computed. Element names, in order (present or absent according to
which of censoring/an offset/covariates/clustering actually
apply -- see \strong{Details} for why, when \code{cluster} is given and
\code{p>0}, two additional, clustered elements are always computed):
\code{"intercepts"} (always present); \code{"intercepts+offset"} (only if
an offset variable is given); \code{"intercepts+x"} (only if \code{p>0}
covariates are present); \code{"intercepts+random effects"} (only if
\code{cluster} is given -- when \code{p>0} this is an extra, covariate-free
clustered fit computed solely to give the next element a correct
baseline; when \code{p==0} it is instead the final element, there
being no \code{x} to add); \code{"intercepts+x+random effects"} (the final
element whenever both \code{cluster} is given and \code{p>0}). The vector
is correspondingly shorter whenever \code{cluster} is not given,
ending at \code{"intercepts+x"} (or just \code{"intercepts"} if \code{p==0}).
\item \code{lpe}: vector of per-observation likelihood probability elements.
An observation's contribution to the log likelihood is the log of
\code{lpe}. Not currently computed when \code{cluster} is given (returned
as \code{NA}; see \strong{Details}).
\item \code{non.slopes}: number of intercepts in model
\item \code{interceptRef}: the index of the middle (median) intercept used
in computing the linear predictor and \code{var}
\item \code{linear.predictors}: the linear predictor using the first
intercept. When \code{cluster} is given, this is the fixed-effects-only
linear predictor -- random effects are not added in, so that
downstream fit statistics retain their ordinary interpretation.
\item \code{penalty.matrix}: see above
\item \code{info.matrix}: see \code{\link[=orm]{orm()}} and \code{\link[=infoMxop]{infoMxop()}}. When \code{cluster} is
given, this is the corrected (Louis's-identity-adjusted) sparse
information matrix described in \strong{Details}, with additional
row(s)/column(s) in its \code{b}/\code{ab} pieces: a single \code{log(sigma)}
element when \code{mre} is not given, or \code{log(sigma)} and \code{sigma2}
together when it is. Use \code{infoMxop(fit$info.matrix, i='sigma_parameters')} to retrieve the variance of \code{log(sigma)}
alone, or the \verb{2x2} covariance matrix of \code{log(sigma)} and
\code{sigma2} when both are present.
\item \code{ncluster}: number of distinct clusters. Present only when
\code{cluster} is given.
\item \code{nAGQ}: the number of adaptive Gauss-Hermite quadrature nodes
used for the final, converged fit (after any automatic
escalation). Present only when \code{cluster} is given.
\item \code{sigma}: the estimated random-intercept standard deviation.
Present only when \code{cluster} is given \emph{and} \code{mre} is not; when
\code{mre} is given, \code{sigma1} and \code{sigma2} are reported instead (see
below) and \code{sigma} is absent.
\item \code{sigma1}, \code{sigma2}: the two estimated random-effect scale
parameters described in \strong{Details} (\code{sigma1} constrained
positive, \code{sigma2} unconstrained). Present only when both
\code{cluster} and \code{mre} are given.
\item \code{gamma}: vector of estimated (posterior mode) random intercepts,
one per cluster, in the order of the distinct levels of
\code{cluster}. Present only when \code{cluster} is given.
}
}
\description{
Fits ordinal cumulative probability models for continuous or ordinal
response variables, efficiently allowing for a large number of
intercepts by capitalizing on the information matrix being sparse.
Five different distribution functions are implemented, with the
default being the logistic (yielding the proportional odds model).
Penalized estimation and weights are also implemented, as in
\code{\link[=lrm.fit]{lrm.fit()}}. The optimization method is Newton-Raphson with
step-halving, or the Levenberg-Marquardt method. The latter has
been shown to converge better when there are large offsets, or when
the likelihood surface is moderately ill-conditioned (see
\strong{Details}). Execution time is fast even for hundreds of thousands
of intercepts. The limiting factor is the number of intercepts
times the number of columns of \code{x}.

\code{orm.fit} also fits a single-level random-intercept extension of
this model when \code{cluster} is specified, for clustered or repeated-
measures data, optionally with a user-specified, continuously- or
discretely-varying weight on the random effect (see \code{mre} below and
\strong{Details}). See \strong{Details} for a full description of the
random-intercept model, how it is fit, and how sparseness is
preserved even with thousands of intercepts.
}
\details{
\subsection{Random-intercept model}{

When \code{cluster} is given, \code{orm.fit} fits \code{k} ordinal intercepts and
\code{p} regression coefficients as usual, plus a normally-distributed
random intercept \code{gamma_j ~ N(0, sigma^2)} for each of the distinct
clusters (levels of \code{cluster}). Fitting integrates out the random
effects to obtain the marginal likelihood, using adaptive
Gauss-Hermite quadrature (AGQ) rather than a Laplace approximation.
This choice was empirically driven: extensive validation against
\code{ordinal::clmm2} showed that Laplace approximation (equivalent to
\code{nAGQ=1}) produces substantial, systematic bias in both the fixed
effects and, especially, the variance component -- worsening as
cluster informativeness decreases (small clusters, large \code{sigma}),
which is the regime most common in practice. \code{nAGQ} is not held
fixed either: it is escalated automatically, refitting (warm-
started) at each successively larger value in \code{nAGQ.grid} until the
log-likelihood stabilizes within \code{nAGQ.tol}, since no single fixed
value of \code{nAGQ} was found to be adequate across the full range of
\code{sigma} a user might encounter.
}

\subsection{Weighted random effects via \code{mre}}{

When a fixed, user-specified per-observation vector \code{mre} is also
given, the random intercept's own contribution to the linear
predictor is no longer a constant \code{gamma_j} -- it is scaled by a
quantity that can vary, continuously if desired, across an
individual cluster's own observations:
\deqn{\eta_{ij} = \ldots + [\sigma_1(1-\mbox{mre}_{ij}) +
  \sigma_2\,\mbox{mre}_{ij}]\, v_i}
where \code{v_i ~ N(0,1)} is a standardized, per-cluster random effect
and \code{sigma1}, \code{sigma2} are two estimated scale parameters --
\code{sigma1} constrained positive via a \code{log} link (to pin down \code{v_i}'s
otherwise arbitrary sign, since flipping the sign of both \code{sigma2}
and every \code{v_i} simultaneously leaves the likelihood unchanged
unless something anchors it), \code{sigma2} left unconstrained, so it
may legitimately come out negative. \code{mre} is entirely user-computed
and fixed -- it is data, exactly like a column of \code{x}, never
estimated -- while \code{sigma1} and \code{sigma2} are estimated jointly with
\code{alpha} and \code{beta} in the same Newton step. \code{mre=NULL} (the
default) recovers the ordinary, single-\code{sigma} model exactly, with
no loss of speed or accuracy.

This additive form -- \code{sigma1} and \code{sigma2} each multiplying a
separately-known quantity, \verb{(1-mre)v_i} and \verb{mre \\, v_i}
respectively -- was chosen over an earlier, multiplicative design
(a single \code{sigma} combined with a separately estimated multiplier)
specifically because it keeps \code{sigma1} and \code{sigma2} close to
orthogonal. Extensive testing found the multiplicative form's extra
parameter became structurally unidentifiable whenever the base
\code{sigma} approached its lower boundary of zero, causing the fit to
converge to an arbitrary, meaningless value for it; \code{sigma1} and
\code{sigma2} remain well-behaved instead (their estimated correlation
stayed well under \code{0.2} in magnitude across every scenario tested,
including deliberately small samples and scale parameters of
opposite sign), since each is an absolute scale directly identified
by its own share of the data rather than a ratio relative to the
other.

\code{mre} must vary \emph{within} at least some clusters for \code{sigma1} and
\code{sigma2} to be separately identifiable; \code{orm.fit} checks this
directly and stops with an informative error if \code{mre} has no
variation anywhere in the data (e.g. is constant at \code{0}, at \code{1}, or
at any other single value for every observation). A cluster
contributing only a single observation is harmless and needs no
special handling -- it simply contributes nothing toward separating
\code{sigma1} from \code{sigma2}, the same way a covariate with no variation
in one subgroup does not hurt an ordinary regression, as long as
\emph{some} clusters elsewhere in the data do have within-cluster \code{mre}
variation.

A natural and recommended choice for a Markov-1 model on repeated
ordinal responses (a proportional-odds model whose linear predictor
includes a term for the previous response) is a piecewise-constant
\code{mre}: \code{0} at each subject's first follow-up time and \code{1} at every
subsequent one (see \strong{Examples}). The motivation is that a plain,
constantly-weighted random effect double-counts its own influence
once a lag term is present -- the previous response already carries
the random effect's influence forward, so adding it again at full
strength at every subsequent visit can induce an unrealistically
large or ever-growing correlation across time. Letting \code{sigma2}
differ from \code{sigma1} -- including, if the data call for it, being
of the opposite sign -- lets the random effect's influence after
the first visit either add to or subtract from the correlation the
lag term alone would induce, as the data determine. \code{mre} is not
restricted to this \code{0}/\code{1} step function, however; any fixed,
user-computed function of the data (continuously varying with
follow-up time, for example) is equally valid.
}

\subsection{Likelihood ratio tests under clustering}{

When \code{cluster} is given together with \code{p>0} covariates, \code{orm.fit}
fits an \emph{additional}, otherwise-unnecessary clustered model:
intercepts plus the same random-effects structure (including \code{mre},
if given), but without \code{x}. This provides a clustered null deviance
that is directly comparable to the clustered full model's deviance,
so that the reported model likelihood ratio chi-square is a genuine
test of the covariates given the random-effects structure already
in the model -- not a test confounded with whether the random
effect itself improves the fit. See the \code{deviance} element of
\strong{Returns} for the full, named composition of the returned
deviance vector.
}

\subsection{Inner and outer loops}{

Fitting uses an outer, block-alternating loop, each iteration of
which does the following:
\enumerate{
\item \strong{Per-cluster mode-finding} (the inner loop): a scalar
Newton-Raphson iteration finds each cluster's own posterior mode
and curvature for its random intercept, at the current
\verb{(alpha, beta, sigma)}. This is vectorized across \emph{all} clusters
simultaneously -- one lean Fortran call per Newton step, not one
call per cluster -- so its cost is \code{O(n)} regardless of the
number of clusters.
\item \strong{AGQ-reweighted Newton step for \verb{(alpha, beta)}}: at each of
the current \code{nAGQ} quadrature nodes (centered and scaled per
cluster using the mode and curvature just found), the same,
completely unmodified \code{orm.fit} likelihood/gradient/Hessian
machinery is called once, with that node's random-intercept
value folded into the offset and its posterior node weight
folded into the case weights. The \code{nAGQ} results are summed to
form one Newton step. Because this reuses \code{orm.fit}'s own
tri-band (or, under interval censoring, general sparse triplet)
intercept-Hessian representation and its \code{O(k)} solve unchanged,
the random-intercept extension inherits the same scalability to
very large numbers of intercepts that \code{orm.fit} has without
random effects -- it simply pays this cost \code{nAGQ} times per
outer iteration instead of once.
\item \strong{Fitting the random-effect scale parameter(s)}. When \code{mre} is
not given, this is a one-dimensional profile search for \code{sigma}
via \code{optimize()}, using only the lean, \code{O(n)} per-observation
routine from step 1 (no gradient or Hessian needed) -- cheap
relative to step 2. This separate-search design was tried for
the \code{sigma1}/\code{sigma2} case too, but found to converge to a
persistent wrong value whenever the true \code{sigma2} had the
opposite sign from its (necessarily sign-unknown-in-advance)
starting value, regardless of sample size -- confirmed to be a
genuine optimization-path problem, not a sign of poor
identifiability, since a direct grid-scan of the likelihood
always found a well-behaved optimum near the true values. When
\code{mre} \emph{is} given, \code{sigma1} and \code{sigma2} are therefore instead
estimated jointly with \code{alpha} and \code{beta} in the very same
Newton step as step 2, \code{sigma2} started at exactly \code{0} (not an
arbitrary nonzero value) so the step itself discovers the
correct sign directly from the data rather than being biased
toward whichever sign it happened to start on.
}

Because this scheme converges only linearly near the optimum
(unlike \code{orm.fit}'s own Newton-Raphson, which converges
quadratically), its convergence criteria differ correspondingly:
the gradient tolerance is used as an absolute, not an \code{n}-scaled,
criterion (an \code{n}-scaled tolerance is nearly a no-op at realistic
sample sizes for a scheme with only linear convergence), and a
separate allowance accepts convergence once \code{sigma}'s absolute
change falls below a small floor even if its \emph{relative} change has
not, since relative changes become numerically meaningless once
\code{sigma} is already effectively at its lower boundary of zero.

The outer loop's own convergence is accelerated using SQUAREM
(Varadhan and Roland, 2008), a generic extrapolation method for
fixed-point iterations that share this scheme's linear/geometric
convergence pattern. Every SQUAREM cycle runs two ordinary outer
iterations, uses them to estimate the local geometric contraction
rate, and extrapolates a candidate point that would take many more
ordinary iterations to reach directly. That candidate is never
accepted on faith: it is itself run through one more ordinary outer
iteration ("stabilization"), and is kept only if that step's own
parameter change is no larger than the plain, unaccelerated
iteration's would have been -- so acceleration can only match or
improve on plain iteration, never meaningfully underperform it. The
extrapolation's step length is additionally bounded by an adaptive
cap that starts conservative and grows after each accepted
extrapolation, shrinking back after a rejected one -- an unbounded
step length is only trustworthy once the local trajectory has
settled into a clean geometric pattern, and can otherwise produce
wild, wastefully expensive trial points. In testing this combination
reduced wall clock fitting time by roughly 15-20\% relative to
unbounded extrapolation, and reduced the number of outer iterations
needed by roughly 3-to-7-fold relative to no acceleration at all, on
both a very small (20-observation, 10-cluster) and a more
typical-scale (400-observation, 200-cluster) dataset, while landing
at coefficient estimates matching plain (unaccelerated) iteration to
well within the convergence tolerance itself.
}

\subsection{Preserving sparseness}{

Every piece of the random-intercept computation was designed so
that its cost scales with \code{n}, the number of clusters, or cluster
size -- never with \code{k^2} or \code{k^3}:
\itemize{
\item The per-cluster mode-finding and AGQ node evaluation touch \code{k}
only as a lookup into \code{alpha}, never as a matrix dimension.
\item The \verb{(alpha, beta)} Newton step reuses \code{orm.fit}'s existing
tri-band (no interval censoring) or general sparse triplet
(interval censoring present) representation of the intercept
Hessian and its \code{O(k)} tridiagonal solve, completely unchanged.
\item The final, corrected information matrix's missing-information
correction (via Louis's (1982) identity, which corrects for the
variance a fixed-posterior-weight Hessian omits) is accumulated
as a sparse triplet list: each cluster contributes only to the
alpha indices \emph{that cluster's own observations actually touch} --
at most about twice its own size, however large \code{k} is. The
total number of nonzero entries this produces is bounded by
roughly the number of clusters times the square of a typical
cluster's size, independent of \code{k}, for realistic (bounded
cluster size) designs -- confirmed empirically (e.g. 588 nonzero
entries out of 11,175 possible for a \code{k=149}, 75-cluster
example). This lets \code{orm.fit} fit random-intercept models with
thousands of intercepts in about a second, versus tens of seconds
to minutes for \code{ordinal::clmm2} on the same data.
\item \code{log(sigma)}'s standard error (or the covariance of \code{log(sigma)}
and \code{sigma2}, when \code{mre} is given) is obtained by extending only
the non-intercept (\code{b}/\code{ab}) pieces of \code{info.matrix} by one or two
rows and columns -- the intercept-intercept block is never
touched by this extension, so \code{\link[=infoMxop]{infoMxop()}} and other code that
consumes \code{info.matrix} require no special handling for the
random-effects case beyond what interval censoring already
required. \code{\link[=infoMxop]{infoMxop()}} locates these elements by searching
\code{info.matrix$xname} for \code{"log(sigma)"} and \code{"sigma2"} wherever
they occur, rather than assuming a fixed position, so their
presence and order elsewhere in \code{xname} does not matter.
}
}

\subsection{Other computational details}{
\itemize{
\item \code{opt_method='LM'} uses an \emph{asymmetric} Levenberg-Marquardt damping
schedule (\code{lambda} divided by 1.5 on an accepted step, multiplied
by 10 on a rejected one) rather than a symmetric \verb{/10}/\verb{*10} rule.
The symmetric rule can enter a permanent limit cycle on
moderately ill-conditioned (but not literally singular)
likelihood surfaces -- alternating forever between a damping
level that barely succeeds and one that barely fails, with the
true optimum never actually reached. The asymmetric rule was
confirmed, by direct inspection of \code{lambda}'s trajectory on such
a case, to break this cycle, at no cost in iterations for
well-conditioned problems.
\item Fitted standard errors (via \code{\link[=infoMxop]{infoMxop()}} operating on
\code{info.matrix}) always come from this corrected, not naive,
information matrix when random effects are present -- ignoring
the missing-information correction was found in validation
studies to understate intercept standard errors by as much as
40\%.
\item Requesting \code{i='sigma_parameters'} from \code{\link[=infoMxop]{infoMxop()}} can encounter
a genuinely singular information matrix when a fit has converged
with \code{sigma} (or \code{sigma1}) at or very near its lower boundary of
zero -- at that boundary its information content correctly
collapses to zero (a known, non-regular feature of variance-
component estimation, not a numerical defect), and \code{infoMxop}
returns \code{NA} (a scalar, or an all-\code{NA} \verb{2x2} matrix when \code{sigma2}
is also present) with an explanatory warning rather than raising
a raw singularity error in that case.
\item The effective-sample-size adjustment for censored observations
(contributing to the \code{"ESS"} element of \code{stats}) is not yet
available when \code{cluster} is specified; \code{stats["ESS"]} reflects
only the uncensored-observation count in that case, and a warning
is issued.
}
}
}
\examples{
\dontrun{
# Fit an additive logistic model containing numeric predictors age,
# blood.pressure, and sex, assumed to be already properly coded and
# transformed
#
# fit <- orm.fit(cbind(age, blood.pressure, sex), death)

# Fit a random-intercept version for repeated measurements on
# subject id
# fit <- orm.fit(cbind(age, blood.pressure, sex), death, cluster=id)

# A Markov-1 model on repeated ordinal responses, where the linear
# predictor already includes a term for the previous response (e.g.
# via an added column of x, such as a spline in the lagged y). The
# recommended mre for this situation takes on only the values 0 and
# 1: 0 at each subject's FIRST follow-up time, 1 at every later one.
# This lets the fit determine, via the separately-estimated sigma2,
# how much the random effect should add to or subtract from the
# correlation the lag term alone induces, rather than assuming (as
# a plain, constantly-weighted random effect implicitly would) that
# its influence should be injected again at full, undiminished
# strength at every subsequent visit:
# mre <- ave(time, id, FUN = function(t) as.numeric(t > min(t)))
# fit <- orm.fit(cbind(prev.y, age, blood.pressure, sex), y,
#                 cluster=id, mre=mre)
}
}
\seealso{
\code{\link[=orm]{orm()}}, \code{\link[=lrm]{lrm()}}, \code{\link[=glm]{glm()}}, \code{\link[=gIndex]{gIndex()}}, \code{\link[SparseM:solve]{SparseM::solve()}},
\code{\link[=recode2integer]{recode2integer()}}, \code{\link[=infoMxop]{infoMxop()}}
}
\author{
Frank Harrell\cr
Department of Biostatistics, Vanderbilt University\cr
fh@fharrell.com
}
\concept{logistic regression model}
\keyword{models}
\keyword{regression}
